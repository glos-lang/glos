package io

import (
	"C"
	"fmt"
)

pub fn write(s []u8) {
	C::fwrite(<rawptr> s.data, s.count, sizeof<u8>, C::stdout)
}

struct File {
	c rawptr
}

pub fn (f &File) write(bytes []u8) {
	C::fwrite(<rawptr> bytes.data, bytes.count, sizeof<u8>, f.c)
}

pub var stdout = File {c: C::stdout}
