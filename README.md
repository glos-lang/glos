# Glos
> [!WARNING]
> This repo is WIP.

## Quick Start
```console
$ clang -o glos src/*.c
$ ./glos -r sample.glos
```

## Dependencies
The compiler depends on the `clang` executable.

## Platforms supported
- x86_64 Linux
- x86_64 Windows
- ARM64 macOS
