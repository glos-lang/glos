package main

import "fmt"

fn zero[T](ptr &T) {
	extern {
		fn memset(ptr rawptr, ch i32, count u64) rawptr
	}

	memset(<rawptr> ptr, 0, sizeof<T>)
}

fn main() {
	var x = 69
	fmt::println(x)

	zero(&x)
	fmt::println(x)
}
