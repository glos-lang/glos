main :: () {
    source :: "Hello, world!"
    println([]u8(source.data, 5))

    foo :: () -> &u8, s64 { return source.data, source.count }
    println([]u8(foo()))
}

foo :: (s: &u8) -> string {
    return string([]u8(s, strlen(s)))
    extern { strlen :: (s: &u8) -> u64 }
}
