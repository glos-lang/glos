main :: () {
    source :: "Hello, world!"
    println([]char(source.data, 5))

    foo :: () -> &char, s64 { return source.data, source.count }
    println([]char(foo()))
}

foo :: (s: &char) -> string {
    return string([]char(s, strlen(s)))
    extern { strlen :: (s: &char) -> u64 }
}
