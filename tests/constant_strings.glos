package main

import "fmt"

const A = "Hello, world!"
const B = "Foo " + "Bar"
const C = ("Foo " + "Bar").count
const D = "Foo Bar Baz"[4..7]
const E = "HEHE"[1]

fn main() {
	fmt::println(A)
	fmt::println(B)
    fmt::println(C)
	fmt::println(D)
    fmt::println(E)
}
