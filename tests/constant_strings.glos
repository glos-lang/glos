fn println(s str) {
    extern fn putchar(ch i32) i32

    for var i = 0; i < s.count; i += 1 {
        putchar(<i32> s[i])
    }

    putchar(<i32> '\n')
}

const A = "Hello, world!"
const B = "Foo " + "Bar"

fn main() {
    println(A)
    println(B)
}
