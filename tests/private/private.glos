#private
foo :: () {
    println(69)
}

extern {
    #private
    puts :: (s: &u8) -> s32
    printf :: (s: &u8, ...) -> s32
}

#private
extern {
    fputs :: (s: &u8, f: File) -> s32
    fputc :: (c: s32, f: File) -> s32
}
