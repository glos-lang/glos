#private
foo :: () {
    print 69
}

extern {
    #private
    puts :: (s: &char) -> i32
    printf :: (s: &char, ...) -> i32
}

#private
extern {
    fputs :: (s: &char, f: File) -> i32
    fputc :: (c: i32, f: File) -> i32
}
