main :: () {
    extern {
        // Wrongly describing 'puts' to not return. This is needed to trick the control flow analyzer
        // and trigger the runtime panic.
        puts :: noreturn (s: &u8)
    }

    puts("foo()")
}
