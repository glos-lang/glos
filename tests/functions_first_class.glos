apply :: (x: i64, f: (_: i64)) {
    f(x)
}

map :: (x: i64, f: (_: i64) -> i64) -> i64 {
    return f(x)
}

main :: () {
    foo := () { print 69 }
    foo()

    foo = () { print 420 }
    foo()

    apply(2674, (x: i64) { print x / 2 })
    print map(26695, (x: i64) -> i64 { return x * 3 })
}
