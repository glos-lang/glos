package main

import "fmt"

struct Vec2 {
    x i64
    y i64
}

fn print_vec2(v Vec2) {
    fmt::println(v.x)
    fmt::println(v.y)
}

const X = 34 + 35
const Y = X * 4
const Z = Y + 144

fn main() {
    var v = Vec2 {
        X,
        Z
    }

    print_vec2(v)
}
