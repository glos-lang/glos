Show :: union {
    i64
    string
}

show :: (s: Show) {
    print s.case

    if s == {
    case i64;    print s.(i64)
    case string; fmt.println(s.(string))
    case;        fmt.println("nil")
    }

    fmt :: #import "fmt"
}

main :: () {
    s: Show
    show(s)
    show(Show(69))
    show(Show("Urmom"))
}
