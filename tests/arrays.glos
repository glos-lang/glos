package main

import "fmt"

fn print_i64s(xs []i64) {
    for var i = 0; i < xs.count; i += 1 {
        fmt.println(xs[i])
    }
}

fn main() {
    var xs [6]i64
    for var i = 0; i < xs.count; i += 1 {
        xs[i] = <i64> i * 2
    }
    print_i64s(xs[..])
}
