fn print_i64s(xs [i64]) {
    for var i = 0; i < len(xs); i += 1 {
        print xs[i]
    }
}

fn main() {
    var xs [i64; 6]
    for var i = 0; i < len(xs); i += 1 {
        xs[i] = <i64> i * 2
    }
    print_i64s(xs[..])
}
