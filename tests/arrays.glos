print_i64s :: (xs: []i64) {
    for i := 0; i < xs.count; i += 1 {
        print xs[i]
    }
}

main :: () {
    xs: [6]i64
    print xs.data == &xs[0]
    print xs.count

    for i := 0; i < xs.count; i += 1 {
        xs[i] = i * 2
    }
    print_i64s(xs[..])
}
