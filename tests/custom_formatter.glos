main :: () {
    println(Vec2 {69, 420})

    Vec2 :: struct {
        x, y: s32
    }

    #hook
    format :: (this: &Vec2, w: io.Writer, nested: bool) -> s64, error {
        return wprint(w, "Vec2(\{this.x}, \{this.y})")
    }
}

io :: #import "io"
