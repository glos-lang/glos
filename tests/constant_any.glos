a: any : 69
#if a == {
case i64
    main :: () {
        other()
        println(a)
        x: i64 = a // Auto cast
    }
}

b: any : "Urmom"
#if b == string {
    other :: () {
        println(b)
        x: string = b // Auto cast
    }
}
