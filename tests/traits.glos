Writer :: trait {
    write: (bytes: []u8)
}

greet :: (w: Writer) {
    w.write([]u8("Hello, world!\n"))
}

File :: distinct os.File

main :: () {
    greet(File(os.stdout))
}

write :: (this: File, bytes: []u8) {
    C.fwrite(rawptr(bytes.data), sizeof(u8), u64(bytes.count), os.File(this))
}

C  :: #import "C"
os :: #import "os"
