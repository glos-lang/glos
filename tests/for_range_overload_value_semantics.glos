main :: () {
    m: map[string]s64
    defer m.free()

    m["Foo"] = 69
    m["Bar"] = 420

    for k, v := range m {
        println(k, v)
    }

    for _, v := range &m {
        *v *= 2
    }

    println()
    for k, v := range m {
        println(k, v)
    }
}
