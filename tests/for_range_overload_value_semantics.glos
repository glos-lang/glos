main :: () {
    t: Table(string, s64)
    defer t.free()

    t["Foo"] = 69
    t["Bar"] = 420

    for k, v := range t {
        println(k, v)
    }

    for _, v := range &t {
        *v *= 2
    }

    println()
    for k, v := range t {
        println(k, v)
    }
}
