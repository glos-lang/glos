main :: () {
    a := Vec2{34, 200}
    b := Vec2{35, 220}
    println(a + b)
}

Vec2 :: struct {
    x, y: i32
}

operator + :: (this: Vec2, that: Vec2) -> Vec2 {
    this.x += that.x
    this.y += that.y
    return this
}
