main :: () {
    wprintln(stdout(), "Hi!")

    stdout :: () -> io.Writer {
        w: io.Writer
        w.type = os.File
        w.data = rawptr(&os.stdout)
        w.impl = io.Writer.(os.File)
        return w
    }
}

io :: #import "io"
os :: #import "os"
