package main

import "fmt"

fn foo(x bool) {
    if x {
        fmt.println(1337)
    } else {
        fmt.println(69)
    }
    bar(x)
}

fn bar(x bool) {
    if x {
        fmt.println(80085)
    } else {
        fmt.println(420)
        foo(true)
    }
}

fn main() {
    foo(false)
}
