Value :: union {
    i64
    string
}

main :: () {
    V :: Value(69)
    if V == {
    case string
        println(V)

    case i64
        println(V)

    case null
        unreachable()
    }

    if V == string {
        println(V)
    }
}
